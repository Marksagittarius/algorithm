# 0x04 前缀和

## 引言

前缀和是一种求解数组区间累加问题的常用技巧，能够快速求解连续区间 `[l, r)` 或 `[l, r]` 上的元素累加和。

构造前缀和的一般步骤如下：

```c++
int q[LEN];
int d[LEN];

for (int i = 0; i < n; i++)
    scanf("%d", &q[i]);

for (int i = 1; i <= n; i++)
    d[i] = d[i - 1] + q[i - 1];

```

此时欲求闭区间 `q[l, r] (0 <= l <= r <= n - 1)` 中所有数组元素之和只需计算 `d[r + 1] - d[l]` 即可。

## 例题

在实际解题过程中，不一定要显式构造上述前缀和数组来求解相关问题，只需融汇贯通其思想即可。

[560. 和为 K 的子数组](https://leetcode.cn/problems/subarray-sum-equals-k/description/)

```c++
class Solution {
public:
    int subarraySum(vector<int>& nums, int k) {
        int ans = 0;
        unordered_map<int, int> mp;
        int sum = 0;
        mp[0] = 1;
        for (auto num: nums) {
            sum += num;
            if (mp.count(sum - k))
                ans += mp[sum - k];
            mp[sum]++;
        }
        return ans;
    }
};
```

[1218. 最长定差子序列](https://leetcode.cn/problems/longest-arithmetic-subsequence-of-given-difference/description/?envType=study-plan-v2&envId=dynamic-programming)

```c++
class Solution {
public:
    int longestSubsequence(vector<int>& nums, int diff) {
        int ans = 0;
        unordered_map<int, int> f;
        for (auto& num : nums) {
            f[num] = f[num - diff] + 1;
            ans = max(ans, f[num]);
        }
        return ans;
    }
};
```

前缀和数组（亦可以理解为映射）能够通过作差的方式维护某些有趣的性质。

[525. 连续数组](https://leetcode.cn/problems/contiguous-array/description/)

给定一个二进制数组 `nums` , 找到含有相同数量的 `0` 和 `1` 的最长连续子数组，并返回该子数组的长度。

```c++
class Solution {
public:
    int findMaxLength(vector<int>& nums) {
        int ans = 0;
        int n = nums.size();
        unordered_map<int, int> mp;
        int cnt = 0;
        mp[cnt] = -1;
        for (int i = 0; i < n; i++) {
            int num = nums[i];
            if (num == 1) cnt++; else cnt--;
            if (mp.count(cnt)) {
                ans = max(ans, i - mp[cnt]);
            } else {
                mp[cnt] = i;
            }
        }
        return ans;
    }
};
```

通过观察不难发现，若 `cnt_i == cnt_j && i < j`，则区间 `(i, j]` 之间的 0 和 1 数量必定相等。

[523. 连续的子数组和](https://leetcode.cn/problems/continuous-subarray-sum/description/)

```c++
class Solution {
public:
    bool checkSubarraySum(vector<int>& nums, int k) {
        int n = nums.size();
        if (n < 2) return false;
        unordered_map<int, int> mp;
        mp[0] = -1;
        int rmd = 0;
        for (int i = 0; i < n; i++) {
            rmd = (rmd + nums[i]) % k;
            if (mp.count(rmd)) {
                int idx = mp[rmd];
                if (i - idx >= 2)
                    return true;
            } else {
                mp[rmd] = i;
            }
        }
        return false;
    }
};
```

通过观察不难发现，若 `sum[0, i]` 和 `sum[0, j]` 同余 `k`，则 `nums(i, j]` 整除 `k`。

一维的前缀和可以扩展到二维空间，其构造方式如下：

[304. 二维区域和检索 - 矩阵不可变](https://leetcode.cn/problems/range-sum-query-2d-immutable/description/)

```c++
class NumMatrix {
public:
    vector<vector<int> > pre;
    NumMatrix(vector<vector<int>>& nums) {
        int m = nums.size();
        int n = nums[0].size();
        pre = vector<vector<int> >(m + 1, vector<int>(n + 1));
        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= n; j++) {
                pre[i][j] = nums[i - 1][j - 1] + pre[i][j - 1] + pre[i - 1][j] - pre[i - 1][j - 1];
            }
        }
    }
    
    int sumRegion(int row1, int col1, int row2, int col2) {
        return pre[row2 + 1][col2 + 1] - pre[row2 + 1][col1] - pre[row1][col2 + 1] + pre[row1][col1];
    }
};
```
