# 二分

## 引言

二分体现了「分而治之」的思想，常见的二分技巧有「二分答案」与「二分查找」两种。

### 二分答案

将数组划分为两个连续区间，若其中一个区间中的所有元素均满足某种性质而另一区间中的所有元素均不满足，即可使用二分答案技巧找到两个区间分界之端点。

逼近右半区间的左端点：

```cpp
while (l < r) {
    int mid = (l + r) / 2;
    if (check(mid))
        r = mid;
    else
        l = mid + 1;
}

return l;
```

逼近左半区间的右端点：

```cpp
while (l < r) {
    int mid = (l + r + 1) / 2;

    if (check(mid))
        l = mid;
    else
        r = mid - 1;
}

return r;
```
