# 0x25 最小生成树

## 引言

最小生成树算法中较为容易被实现的 Kruskal 算法其一般构造方式如下：

```c++
#include <bits/stdc++.h>
using namespace std;

int n, m;
const int LEN = 1e6 + 5;

struct node {
    int u, v, w;
} q[LEN];

int cmp(node lhs, node rhs) {
    return lhs.w < rhs.w;
}

int f[LEN];

int find(int x) {
    return x == f[x] ? x : f[x] = find(f[x]);
}

int main() {
    scanf("%d %d", &n, &m);

    for (int i = 0; i < m; i++)
        scanf("%d%d%d", &q[i].u, &q[i].v, &q[i].w);

    for (int i = 0; i <= n; i++)
        f[i] = i;

    sort(q, q + m, cmp);
    int sum = 0, tot = 0;

    for (int i = 0; i < m; i++) {
        int xx = find(q[i].u);
        int yy = find(q[i].v);

        if (xx != yy) {
            f[xx] = yy;
            sum += q[i].w;
            tot++;
        }
    }

    if (tot < n - 1)  {
        printf("orz");
    } else {
        printf("%d", sum);
    }

    return 0;
}
```
