# 0x06 字符串

## 引言

字符串可以被认为是由字符构成的数组，然而，在实际的考查中，字符串往往被赋予其独特的意义，由此诞生了诸多面向字符串问题的精妙算法。

在字符串问题的考查中，回文串的考查始终为重中之重。

[647. 回文子串](https://leetcode.cn/problems/palindromic-substrings/description/) 可使用一种较为粗暴的方式解决：

```c++
class Solution {
public:
    int countSubstrings(string s) {
        int n = s.size();
        int ans = 0;
        for (int i = 0; i < 2 * n - 1; i++) {
            int l = i / 2;
            int r = i / 2 + i % 2;
            while (l >= 0 && r < n && s[l] == s[r]) {
                l--;
                r++;
                ans++;
            }
        }
        return ans;
    }
};
```

该问题存在一个更为高效的算法，其名为 Manacher 算法：

```c++
class Solution {
public:
    int countSubstrings(string s) {
        int n = s.size();
        int ans = 0;
        string str = "$#";
        for (auto& ch : s) {
            str += ch;
            str += '#';
        }
        n = str.size();
        str += '!';

        vector<int> f(n);
        int l = 0, r = 0;
        for (int i = 1; i < n; i++) {
            f[i] = i <= r ? min(r - i + 1, f[2 * l - i]) : 1;
            while (str[i + f[i]] == str[i - f[i]])
                f[i]++;
            if (i + f[i] - 1 > r) {
                l = i;
                r = i + f[i] - 1;
            }
            ans += f[i] / 2;
        }
        return ans;
    }
};
```

该算法通过维护回文中心和最大回文半径，显著提高了枚举的效率。

在字符串问题中有相当多的经典题目需要运用动态规划思想，这些题目的故事将在后续章节中被娓娓道来。
