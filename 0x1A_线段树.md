# 0x1A 线段树

## 引言

线段树是一种深邃的树状结构，能够在指数时间内完成区间运算。

区间加法与区间查询线段树

```c++
class RangeAddArray {
    int n;
    vector<long long> d, lz;

    void push(int p, int s, int t) {
        if (lz[p] == 0) return;
        int m = s + (t - s) / 2;
        d[p*2] += lz[p] * (m - s + 1);
        lz[p*2] += lz[p];
        d[p*2+1] += lz[p] * (t - m);
        lz[p*2+1] += lz[p];
        lz[p] = 0;
    }

public:
    RangeAddArray(vector<int>& nums) {
        n = nums.size();
        d.assign(n * 4, 0); 
        lz.assign(n * 4, 0);
        for(int i = 0; i < n; i++) {
            add(i + 1, i + 1, nums[i], 1, n, 1);
        }
    }

    void add(int l, int r, int val, int s, int t, int p) {
        if (l <= s && t <= r) {
            d[p] += (long long)val * (t - s + 1);
            lz[p] += val;
            return;
        }
        push(p, s, t);
        int m = s + (t - s) / 2;
        if (l <= m) add(l, r, val, s, m, p*2);
        if (r > m) add(l, r, val, m + 1, t, p*2+1);
        d[p] = d[p*2] + d[p*2+1];
    }

    long long query(int l, int r, int s, int t, int p) {
        if (l <= s && t <= r) return d[p];
        push(p, s, t);
        int m = s + (t - s) / 2;
        long long res = 0;
        if (l <= m) res += query(l, r, s, m, p*2);
        if (r > m) res += query(l, r, m + 1, t, p*2+1);
        return res;
    }
};
```

区间修改与区间查询线段树

```c++
class RangeUpdateArray {
    int n;
    vector<long long> d, lz;
    vector<bool> v;

    void push(int p, int s, int t) {
        if (!v[p]) return;
        int m = s + (t - s) / 2;
        d[p*2] = lz[p] * (m - s + 1);
        lz[p*2] = lz[p]; v[p*2] = true;
        d[p*2+1] = lz[p] * (t - m);
        lz[p*2+1] = lz[p]; v[p*2+1] = true;
        v[p] = false; lz[p] = 0;
    }

public:
    RangeUpdateArray(vector<int>& nums) {
        n = nums.size();
        d.assign(n * 4, 0); 
        lz.assign(n * 4, 0);
        v.assign(n * 4, false);
        for(int i = 0; i < n; i++) {
            update(i + 1, i + 1, nums[i], 1, n, 1);
        }
    }

    void update(int l, int r, int val, int s, int t, int p) {
        if (l <= s && t <= r) {
            d[p] = (long long)val * (t - s + 1);
            lz[p] = val; v[p] = true;
            return;
        }
        push(p, s, t);
        int m = s + (t - s) / 2;
        if (l <= m) update(l, r, val, s, m, p*2);
        if (r > m) update(l, r, val, m + 1, t, p*2+1);
        d[p] = d[p*2] + d[p*2+1];
    }

    long long query(int l, int r, int s, int t, int p) {
        if (l <= s && t <= r) return d[p];
        push(p, s, t);
        int m = s + (t - s) / 2;
        long long res = 0;
        if (l <= m) res += query(l, r, s, m, p*2);
        if (r > m) res += query(l, r, m + 1, t, p*2+1);
        return res;
    }
};
```
