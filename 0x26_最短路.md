# 0x26 最短路

## 引言

最短路问题能够求解图中任意两点之间的最短路径长度。

最短路问题最经典的算法是 Dijkstra 算法，其一般构造方式如下：

```c++
#include <bits/stdc++.h>

using namespace std;

const int LEN = 1e6 + 5;
const int INF = 0x3f3f3f3f;
int n, m, s;

struct edge {
    int u, v, w;
    edge(int u, int v, int w): u(u), v(v), w(w) {}
};

struct node {
    int d, u;
    node(int d, int u) : d(d), u(u) {}
    friend bool operator<(node a, node b) { return a.d > b.d; }
};

vector<edge> edges;
vector<int> g[LEN];
int dis[LEN];
int vis[LEN];
int p[LEN];

void dijkstra(int s) {
    priority_queue<node> pq;
    for (int i = 0; i <= n; i++)
        dis[i] = INF;
    memset(vis, 0, sizeof(vis));
    dis[s] = 0;
    pq.push(node(0, s));

    int cnt = 0;
    while (!pq.empty()) {
        auto it = pq.top();
        pq.pop();

        int u = it.u;
        if (vis[u]) continue;

        vis[u] = 1;
        cnt++;

        if (cnt >= n) break;

        for (int i = 0; i < g[u].size(); i++) {
            auto e = edges[g[u][i]];
            if (dis[e.v] > dis[u] + e.w) {
                dis[e.v] = dis[u] + e.w;
                p[e.v] = g[u][i];
                pq.push(node(dis[e.v], e.v));
            }
        }

    }
}

void add_edge(int u, int v, int w) {
    edges.push_back(edge(u, v, w));
    int ss = edges.size();
    g[u].push_back(ss - 1);
}

void init() {
    for (int i = 0; i <= n; i++)
        g[i].clear();

    edges.clear();
}

int main() {
    scanf("%d%d%d", &n, &m, &s);
    init();

    for (int i = 0; i < m; i++) {
        int u, v, w;
        scanf("%d%d%d", &u, &v, &w);
        add_edge(u, v, w);
    }

    dijkstra(s);

    for (int i = 1; i <= n; i++) {
        if (dis[i] != INF)
            printf("%d ", dis[i]);
        else
            printf("%d ", INT_MAX);
    }
SPFA
    return 0;
}
```

Dijkstra 算法虽然天下无敌但是并不能解决负边问题，倘若存在负边可使用 SPFA 算法。
